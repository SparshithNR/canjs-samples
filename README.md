# digitalClock
simple canjs app

This is an sample digital clock app which keeps ticking. To run this locally you have to have can.js, jquery, moment.js in your local system.
If you have npm in your system that also works. just need to run npm install in command prompt. All the required js files will get added to your workign folder
after getting node_modules folder, just run the local host and open the application
